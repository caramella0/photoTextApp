<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Directory principale per i file audio TTS -->
    <external-files-path
        name="tts_audio"
        path="tts_audio/" />

    <!-- Directory fallback per i file audio -->
    <files-path
        name="internal_audio"
        path="/" />

    <!-- Directory cache per file temporanei -->
    <cache-path
        name="cache_audio"
        path="/" />

    <!-- Solo se necessario per compatibilità -->
    <external-files-path
        name="legacy_audio"
        path="Audio/" />
</paths>